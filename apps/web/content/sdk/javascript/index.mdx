---
title: SDK JavaScript / TypeScript
description: Overview of the official CanaryGate SDK for browser and Node.js.
keywords:
  ['javascript sdk', 'typescript sdk', 'js sdk', 'browser sdk', 'node sdk']
aliases: ['javascript sdk', 'typescript sdk', 'js sdk']
searchBoost: 8
asIndexPage: true
---

The `@canarygate/sdk` package is the official client for integrating CanaryGate into JavaScript and TypeScript applications. It is split into two entry points:

- `@canarygate/sdk/client` — for browsers. Always uses the snapshot request and never opens SSE.
- `@canarygate/sdk/server` — for server runtimes (Node.js, Deno, Bun, Edge). Supports real-time SSE streaming with `stream: true`.

There is no root entry point (`@canarygate/sdk` or `@canarygate/sdk`); always import one of the two paths above.

## Compatibility

| Environment                                    | Support      |
| ---------------------------------------------- | ------------ |
| Browser (modern)                               | ✓            |
| Node.js 18+                                    | ✓            |
| Deno                                           | ✓ (via npm)  |
| Bun                                            | ✓            |
| Edge Runtime (Cloudflare Workers, Vercel Edge) | Partial\*    |

\*The client entry (`@canarygate/sdk/client`) always uses the snapshot request and never opens SSE. On server runtimes (Node.js, Deno, Bun, Edge) import `@canarygate/sdk/server` and set `stream: true` to enable streaming via fetch-based SSE.

## Installation

```bash
npm install @canarygate/sdk
```

## Full API

| Method                             | Description                                    |
| ---------------------------------- | ---------------------------------------------- |
| `new CanaryGate(apiKey, options?)` | Creates the client                             |
| `init()`                           | Loads flags via snapshot; starts SSE in server environments with `stream: true` |
| `getFlag(key)`                     | Returns `FlagData \| undefined`                |
| `getFlags()`                       | Returns `FlagData[]` (all flags)               |
| `isStale()`                        | `true` if the connection is degraded           |
| `getLastSyncAt()`                  | `string \| null` of the last sync              |
| `disconnect()`                     | Closes the stream connection (if any) and releases resources |

## Next steps

<Cards>
  <Cards.Card
    title="Initialization"
    href="/docs/sdk/javascript/initialization"
  />
  <Cards.Card
    title="Evaluating flags"
    href="/docs/sdk/javascript/evaluate-flags"
  />
  <Cards.Card
    title="Real-time and SSE"
    href="/docs/sdk/javascript/realtime-sse"
  />
  <Cards.Card
    title="TypeScript types"
    href="/docs/sdk/javascript/typescript-types"
  />
</Cards>

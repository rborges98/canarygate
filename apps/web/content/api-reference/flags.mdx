---
title: Flags API
description: Endpoints for managing flags via REST API.
keywords:
  [
    'flags api',
    'feature flags api',
    'rest api',
    'flag endpoints',
    'manage flags'
  ]
aliases: ['flags endpoint', 'feature flags api']
---

## Base URL

```
https://api.canarygate.io/api/v1
```

## List flags

```http
GET /flags
```

Returns all flags for the environment associated with the API Key.

**Response 200**

```json
[
  {
    "key": "new-checkout",
    "type": "boolean",
    "enabled": true,
    "description": "New checkout flow"
  },
  {
    "key": "new-dashboard",
    "type": "rollout",
    "enabled": false,
    "percentage": 25,
    "description": null
  }
]
```

## Get flag

```http
GET /flags/:key
```

**Response 200**

```json
{
  "key": "new-checkout",
  "type": "boolean",
  "enabled": true,
  "description": "New checkout flow"
}
```

**Response 404**

```json
{
  "error": "NOT_FOUND",
  "message": "Flag 'new-checkout' not found"
}
```

## Create flag

```http
POST /flags
```

**Body**

```json
{
  "key": "new-feature",
  "type": "boolean",
  "description": "My new feature"
}
```

| Field         | Type                     | Required |
| ------------- | ------------------------ | -------- |
| `key`         | `string`                 | Yes      |
| `type`        | `'boolean' \| 'rollout'` | Yes      |
| `description` | `string`                 | No       |

**Response 201**

```json
{
  "key": "new-feature",
  "type": "boolean",
  "enabled": false,
  "description": "My new feature"
}
```

## Update flag

```http
PATCH /flags/:key
```

**Body** (optional fields)

```json
{
  "enabled": true,
  "percentage": 50,
  "description": "Updated description"
}
```

**Response 200** — returns the updated flag.

## Delete flag

```http
DELETE /flags/:key
```

**Response 204** — no body.
